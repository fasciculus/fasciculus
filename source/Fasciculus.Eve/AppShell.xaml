<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Fasciculus.Eve.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:p="clr-namespace:Fasciculus.Eve.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="Fasciculus.Eve">

    <ShellContent
        Title="Loading"
        ContentTemplate="{DataTemplate p:LoadingPage}"
        Route="Loading" />

    <ShellContent
        Title="Info"
        ContentTemplate="{DataTemplate p:InfoPage}"
        Route="Info" />

    <ShellContent
        Title="Industry"
        ContentTemplate="{DataTemplate p:IndustryPage}"
        Route="Industry" />

    <ShellContent
        Title="Market"
        ContentTemplate="{DataTemplate p:MarketPage}"
        Route="Market" />

    <ShellContent
        Title="Map"
        ContentTemplate="{DataTemplate p:MapPage}"
        Route="Map" />

</Shell>
