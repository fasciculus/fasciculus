# Fasciculus.NetStandard

This source-only package contains sources for types not supported by `netstandard2.0`.
All sources are attributed with `internal`.

## Attributes

- `DisallowNullAttribute`

